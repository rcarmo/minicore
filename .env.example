# Use environment overrides with make, or a Compose --env-file; no secrets here.
MINICORE_BIND=127.0.0.1
MINICORE_PORT=19000
MINICORE_EXPOSURE_PROFILE=private
MINICORE_ALLOWED_ORIGINS=http://127.0.0.1:19000
# Authenticated deployment reads secrets/mcp-tokens.json, never values in this file.
