# Operator scenarios

Predefined fault and reset controls exposed only to authenticated MCP God mode through a dedicated fixed-scenario controller. Operator mode cannot discover or invoke them. No arbitrary command or target input is accepted.
